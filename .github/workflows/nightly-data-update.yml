name: Nightly Data Fetch and Update

on:
  schedule:
    # Run every evening at 8 PM UTC (adjust as needed)
    - cron: "0 20 * * *"
  workflow_dispatch:

env:
  AWS_REGION: us-east-1

jobs:
  update-data:
    name: Fetch Data and Update S3
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Get S3 bucket name from Pulumi
        id: get-bucket
        working-directory: ./infrastructure
        env:
          PULUMI_CONFIG_PASSPHRASE: ${{ secrets.PULUMI_CONFIG_PASSPHRASE }}
        run: |
          pip install pulumi pulumi-aws
          BUCKET_NAME=$(pulumi stack output bucket_name)
          echo "BUCKET_NAME=$BUCKET_NAME" >> $GITHUB_OUTPUT

      - name: Download existing data from S3
        run: |
          mkdir -p data
          aws s3 cp s3://${{ steps.get-bucket.outputs.BUCKET_NAME }}/data/bond_yields_10y.csv data/ || echo "No existing bond_yields_10y.csv"
          aws s3 cp s3://${{ steps.get-bucket.outputs.BUCKET_NAME }}/data/stock_indices.csv data/ || echo "No existing stock_indices.csv"
          aws s3 cp s3://${{ steps.get-bucket.outputs.BUCKET_NAME }}/data/currencies.csv data/ || echo "No existing currencies.csv"
          aws s3 cp s3://${{ steps.get-bucket.outputs.BUCKET_NAME }}/data/sentiment_data.csv data/ || echo "No existing sentiment_data.csv"
          aws s3 cp s3://${{ steps.get-bucket.outputs.BUCKET_NAME }}/data/sentiment_data_domestic.csv data/ || echo "No existing sentiment_data_domestic.csv"

      - name: Update sentiment data
        continue-on-error: true
        run: |
          python src/fetch_sentiment.py --update-all

      - name: Update market data
        continue-on-error: true
        run: |
          python src/fetch_data.py update
          python src/fetch_data.py generate

      - name: Upload updated data to S3
        run: |
          aws s3 sync data/ s3://${{ steps.get-bucket.outputs.BUCKET_NAME }}/data/ --exclude ".gitkeep" --no-overwrite --acl public-read
          aws s3 cp src/data.js s3://${{ steps.get-bucket.outputs.BUCKET_NAME }}/src/data.js --content-type "application/javascript" --acl public-read

      - name: Commit and push updated CSV files (optional)
        continue-on-error: true
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git commit -m "chore: update data files from nightly fetch" || echo "No changes to commit"
          git push

      - name: Log update completion
        run: |
          echo "=== Data Update Complete ==="
          echo "Updated files uploaded to S3 bucket: ${{ steps.get-bucket.outputs.BUCKET_NAME }}"
